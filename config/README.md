# Purpose 
This project centralizes configuration data for all services including application configuration. This can have a simple UI to manage configuration data. Some highlights features includes: 
- Versioning: App can choose to use a specific version of configuration data. 
- S3 Storage: The configuration data is stored in S3, which is a distributed object storage service. 
- A thin client helps services retrieve configuration data easily.
## Architecture 
